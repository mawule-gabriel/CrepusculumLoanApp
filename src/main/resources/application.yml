spring:
  datasource:
    url: jdbc:postgresql://dpg-d5eg7nv5r7bs73f2p3sg-a.oregon-postgres.render.com:5432/loanapp_h3a7
    username: loanapp_h3a7_user
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

jwt:
  secret: ${JWT_SECRET:dGhpcy1pcy1hLXNlY3VyZS0yNTYtYml0LXNlY3JldC1rZXktZm9yLWp3dA==}
  access-token-expiration: 86400000
  refresh-token-expiration: 604800000

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:?CLOUDINARY_CLOUD_NAME must be set}
  api-key: ${CLOUDINARY_API_KEY:?CLOUDINARY_API_KEY must be set}
  api-secret: ${CLOUDINARY_API_SECRET:?CLOUDINARY_API_SECRET must be set}
  max-file-size: 10485760
  folder:
    profile-pictures: profile-pictures
  transformation:
    width: 300
    height: 300
    crop: fill
    gravity: face

logging:
  level:
    org.springframework.security: DEBUG

admin:
  phone: ${ADMIN_PHONE:?ADMIN_PHONE must be set}
  password: ${ADMIN_PASSWORD:?ADMIN_PASSWORD must be set}
  full-name: ${ADMIN_FULL_NAME:?ADMIN_FULL_NAME must be set}
  ghana-card: ${ADMIN_GHANA_CARD:?ADMIN_GHANA_CARD must be set}

app:
  password-reset:
    token-expiry-minutes: 60
    frontend-reset-url: ${FRONTEND_URL:https://travel-loan-vista.onrender.com}/reset-password